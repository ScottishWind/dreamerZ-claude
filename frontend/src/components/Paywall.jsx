/**
 * Paywall components — disabled. All courses are now free.
 * Kept as empty stubs for backward compatibility.
 */
export const Paywall = () => null;
export const PaywallBanner = () => null;
export default Paywall;
